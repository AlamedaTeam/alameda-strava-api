export default function VipDashboard() {
  return (
    <div style={{
      background: "black",
      color: "lime",
      height: "100vh",
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      fontFamily: "monospace",
      fontSize: "22px"
    }}>
      ✅ Nueva ruta /vip-dashboard/index.js funcionando correctamente
    </div>
  );
}
